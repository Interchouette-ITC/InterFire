[package]
name = "interfirectl"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "One-shot InterFire CLI over the daemon Unix socket"

[[bin]]
name = "interfirectl"
path = "src/main.rs"

[dependencies]
interfire-proto = { path = "../interfire-proto" }
