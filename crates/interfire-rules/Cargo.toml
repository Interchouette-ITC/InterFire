[package]
name = "interfire-rules"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic application-rule matching and atomic TOML persistence"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
