[package]
name = "interfire-daemon"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Privileged InterFire daemon: IPC, attribution, prompts, NFQUEUE verdicts"

[[bin]]
name = "interfired"
path = "src/main.rs"

[dependencies]
interfire-ebpf = { path = "../interfire-ebpf" }
interfire-proto = { path = "../interfire-proto" }
interfire-rules = { path = "../interfire-rules" }
nfq = { workspace = true }
nix = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
