[package]
name = "interfire-proto"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Versioned Unix-socket IPC framing for InterFire daemon and clients"

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = { workspace = true }
